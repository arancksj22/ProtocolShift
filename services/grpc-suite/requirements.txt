grpcio==1.63.0
grpcio-tools==1.63.0
grpcio-reflection==1.63.0

# protobuf version is NOT pinned — grpcio-tools 1.63.0 requires >=5.26.1,<6.0
# and will pull the right version automatically
protobuf>=5.26.1,<6.0

# PostgreSQL
asyncpg==0.29.0

# MongoDB
motor==3.4.0

# Redis
redis[asyncio]==5.0.3

# Observability (Prometheus metrics exposed on a sidecar HTTP port)
prometheus-client==0.20.0

# Utilities
python-dotenv==1.0.1
